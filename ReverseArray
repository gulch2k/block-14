// with a for loop,
// create a program that creates a new array in reverse order

const array = [20, 50, 30, 60, 200];
const arrayReverse = [];

for (let i = array.length - 1; i >= 0; i--) {
    arrayReverse.push(array[i]);
}

console.log(arrayReverse);
